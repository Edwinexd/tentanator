# Copy to .env and fill in. Both are optional: without them, manual grading and
# random sampling work; embeddings (maximin sampling, the global question bank /
# auto-match) and AI suggestions need them.
OPENAI_API_KEY=
CEREBRAS_API_KEY=
